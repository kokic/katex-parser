///|
fn builtin_math_symbol_group_0(name : String) -> MathSymbolGroup? {
  match name {
    "-" => Some(BinarySymbol)
    ":" => Some(RelationSymbol)
    "*" => Some(BinarySymbol)
    "\\@gvertneqq" => Some(RelationSymbol)
    "\\@lvertneqq" => Some(RelationSymbol)
    "\\@ngeqq" => Some(RelationSymbol)
    "\\@ngeqslant" => Some(RelationSymbol)
    "\\@nleqq" => Some(RelationSymbol)
    "\\@nleqslant" => Some(RelationSymbol)
    "\\@not" => Some(RelationSymbol)
    "\\@nshortmid" => Some(RelationSymbol)
    "\\@nshortparallel" => Some(RelationSymbol)
    "\\@nsubseteqq" => Some(RelationSymbol)
    "\\@nsupseteqq" => Some(RelationSymbol)
    "\\@varsubsetneq" => Some(RelationSymbol)
    "\\@varsubsetneqq" => Some(RelationSymbol)
    "\\@varsupsetneq" => Some(RelationSymbol)
    "\\@varsupsetneqq" => Some(RelationSymbol)
    "\\amalg" => Some(BinarySymbol)
    "\\And" => Some(BinarySymbol)
    "\\approx" => Some(RelationSymbol)
    "\\approxeq" => Some(RelationSymbol)
    "\\ast" => Some(BinarySymbol)
    "\\asymp" => Some(RelationSymbol)
    "\\backepsilon" => Some(RelationSymbol)
    "\\backsim" => Some(RelationSymbol)
    "\\backsimeq" => Some(RelationSymbol)
    "\\barwedge" => Some(BinarySymbol)
    "\\because" => Some(RelationSymbol)
    "\\between" => Some(RelationSymbol)
    "\\bigcirc" => Some(BinarySymbol)
    "\\bigtriangledown" => Some(BinarySymbol)
    "\\bigtriangleup" => Some(BinarySymbol)
    "\\blacktriangleleft" => Some(RelationSymbol)
    "\\blacktriangleright" => Some(RelationSymbol)
    "\\bowtie" => Some(RelationSymbol)
    "\\boxdot" => Some(BinarySymbol)
    "\\boxminus" => Some(BinarySymbol)
    "\\boxplus" => Some(BinarySymbol)
    "\\boxtimes" => Some(BinarySymbol)
    "\\bullet" => Some(BinarySymbol)
    "\\bumpeq" => Some(RelationSymbol)
    "\\Bumpeq" => Some(RelationSymbol)
    "\\cap" => Some(BinarySymbol)
    "\\Cap" => Some(BinarySymbol)
    "\\cdot" => Some(BinarySymbol)
    "\\centerdot" => Some(BinarySymbol)
    "\\circ" => Some(BinarySymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_1(name : String) -> MathSymbolGroup? {
  match name {
    "\\circeq" => Some(RelationSymbol)
    "\\circlearrowleft" => Some(RelationSymbol)
    "\\circlearrowright" => Some(RelationSymbol)
    "\\circledast" => Some(BinarySymbol)
    "\\circledcirc" => Some(BinarySymbol)
    "\\circleddash" => Some(BinarySymbol)
    "\\cong" => Some(RelationSymbol)
    "\\cup" => Some(BinarySymbol)
    "\\Cup" => Some(BinarySymbol)
    "\\curlyeqprec" => Some(RelationSymbol)
    "\\curlyeqsucc" => Some(RelationSymbol)
    "\\curlyvee" => Some(BinarySymbol)
    "\\curlywedge" => Some(BinarySymbol)
    "\\curvearrowleft" => Some(RelationSymbol)
    "\\curvearrowright" => Some(RelationSymbol)
    "\\dagger" => Some(BinarySymbol)
    "\\dashleftarrow" => Some(RelationSymbol)
    "\\dashrightarrow" => Some(RelationSymbol)
    "\\dashv" => Some(RelationSymbol)
    "\\ddagger" => Some(BinarySymbol)
    "\\diamond" => Some(BinarySymbol)
    "\\div" => Some(BinarySymbol)
    "\\divideontimes" => Some(BinarySymbol)
    "\\doteq" => Some(RelationSymbol)
    "\\Doteq" => Some(RelationSymbol)
    "\\doteqdot" => Some(RelationSymbol)
    "\\dotplus" => Some(BinarySymbol)
    "\\doublebarwedge" => Some(BinarySymbol)
    "\\doublecap" => Some(BinarySymbol)
    "\\doublecup" => Some(BinarySymbol)
    "\\downarrow" => Some(RelationSymbol)
    "\\Downarrow" => Some(RelationSymbol)
    "\\downdownarrows" => Some(RelationSymbol)
    "\\downharpoonleft" => Some(RelationSymbol)
    "\\downharpoonright" => Some(RelationSymbol)
    "\\eqcirc" => Some(RelationSymbol)
    "\\eqsim" => Some(RelationSymbol)
    "\\eqslantgtr" => Some(RelationSymbol)
    "\\eqslantless" => Some(RelationSymbol)
    "\\equiv" => Some(RelationSymbol)
    "\\fallingdotseq" => Some(RelationSymbol)
    "\\frown" => Some(RelationSymbol)
    "\\ge" => Some(RelationSymbol)
    "\\geq" => Some(RelationSymbol)
    "\\geqq" => Some(RelationSymbol)
    "\\geqslant" => Some(RelationSymbol)
    "\\gets" => Some(RelationSymbol)
    "\\gg" => Some(RelationSymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_2(name : String) -> MathSymbolGroup? {
  match name {
    "\\ggg" => Some(RelationSymbol)
    "\\gggtr" => Some(RelationSymbol)
    "\\gnapprox" => Some(RelationSymbol)
    "\\gneq" => Some(RelationSymbol)
    "\\gneqq" => Some(RelationSymbol)
    "\\gnsim" => Some(RelationSymbol)
    "\\gt" => Some(RelationSymbol)
    "\\gtrapprox" => Some(RelationSymbol)
    "\\gtrdot" => Some(BinarySymbol)
    "\\gtreqless" => Some(RelationSymbol)
    "\\gtreqqless" => Some(RelationSymbol)
    "\\gtrless" => Some(RelationSymbol)
    "\\gtrsim" => Some(RelationSymbol)
    "\\hookleftarrow" => Some(RelationSymbol)
    "\\hookrightarrow" => Some(RelationSymbol)
    "\\imageof" => Some(RelationSymbol)
    "\\in" => Some(RelationSymbol)
    "\\intercal" => Some(BinarySymbol)
    "\\Join" => Some(RelationSymbol)
    "\\land" => Some(BinarySymbol)
    "\\le" => Some(RelationSymbol)
    "\\leadsto" => Some(RelationSymbol)
    "\\leftarrow" => Some(RelationSymbol)
    "\\Leftarrow" => Some(RelationSymbol)
    "\\leftarrowtail" => Some(RelationSymbol)
    "\\leftharpoondown" => Some(RelationSymbol)
    "\\leftharpoonup" => Some(RelationSymbol)
    "\\leftleftarrows" => Some(RelationSymbol)
    "\\leftrightarrow" => Some(RelationSymbol)
    "\\Leftrightarrow" => Some(RelationSymbol)
    "\\leftrightarrows" => Some(RelationSymbol)
    "\\leftrightharpoons" => Some(RelationSymbol)
    "\\leftrightsquigarrow" => Some(RelationSymbol)
    "\\leftthreetimes" => Some(BinarySymbol)
    "\\leq" => Some(RelationSymbol)
    "\\leqq" => Some(RelationSymbol)
    "\\leqslant" => Some(RelationSymbol)
    "\\lessapprox" => Some(RelationSymbol)
    "\\lessdot" => Some(BinarySymbol)
    "\\lesseqgtr" => Some(RelationSymbol)
    "\\lesseqqgtr" => Some(RelationSymbol)
    "\\lessgtr" => Some(RelationSymbol)
    "\\lesssim" => Some(RelationSymbol)
    "\\lhd" => Some(BinarySymbol)
    "\\ll" => Some(RelationSymbol)
    "\\Lleftarrow" => Some(RelationSymbol)
    "\\lll" => Some(RelationSymbol)
    "\\llless" => Some(RelationSymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_3(name : String) -> MathSymbolGroup? {
  match name {
    "\\lnapprox" => Some(RelationSymbol)
    "\\lneq" => Some(RelationSymbol)
    "\\lneqq" => Some(RelationSymbol)
    "\\lnsim" => Some(RelationSymbol)
    "\\longleftarrow" => Some(RelationSymbol)
    "\\Longleftarrow" => Some(RelationSymbol)
    "\\longleftrightarrow" => Some(RelationSymbol)
    "\\Longleftrightarrow" => Some(RelationSymbol)
    "\\longmapsto" => Some(RelationSymbol)
    "\\longrightarrow" => Some(RelationSymbol)
    "\\Longrightarrow" => Some(RelationSymbol)
    "\\looparrowleft" => Some(RelationSymbol)
    "\\looparrowright" => Some(RelationSymbol)
    "\\lor" => Some(BinarySymbol)
    "\\Lsh" => Some(RelationSymbol)
    "\\lt" => Some(RelationSymbol)
    "\\ltimes" => Some(BinarySymbol)
    "\\mapsto" => Some(RelationSymbol)
    "\\mid" => Some(RelationSymbol)
    "\\models" => Some(RelationSymbol)
    "\\mp" => Some(BinarySymbol)
    "\\multimap" => Some(RelationSymbol)
    "\\ncong" => Some(RelationSymbol)
    "\\nearrow" => Some(RelationSymbol)
    "\\ngeq" => Some(RelationSymbol)
    "\\ngtr" => Some(RelationSymbol)
    "\\ni" => Some(RelationSymbol)
    "\\nleftarrow" => Some(RelationSymbol)
    "\\nLeftarrow" => Some(RelationSymbol)
    "\\nleftrightarrow" => Some(RelationSymbol)
    "\\nLeftrightarrow" => Some(RelationSymbol)
    "\\nleq" => Some(RelationSymbol)
    "\\nless" => Some(RelationSymbol)
    "\\nmid" => Some(RelationSymbol)
    "\\nparallel" => Some(RelationSymbol)
    "\\nprec" => Some(RelationSymbol)
    "\\npreceq" => Some(RelationSymbol)
    "\\nrightarrow" => Some(RelationSymbol)
    "\\nRightarrow" => Some(RelationSymbol)
    "\\nsim" => Some(RelationSymbol)
    "\\nsubseteq" => Some(RelationSymbol)
    "\\nsucc" => Some(RelationSymbol)
    "\\nsucceq" => Some(RelationSymbol)
    "\\nsupseteq" => Some(RelationSymbol)
    "\\ntriangleleft" => Some(RelationSymbol)
    "\\ntrianglelefteq" => Some(RelationSymbol)
    "\\ntriangleright" => Some(RelationSymbol)
    "\\ntrianglerighteq" => Some(RelationSymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_4(name : String) -> MathSymbolGroup? {
  match name {
    "\\nvdash" => Some(RelationSymbol)
    "\\nvDash" => Some(RelationSymbol)
    "\\nVdash" => Some(RelationSymbol)
    "\\nVDash" => Some(RelationSymbol)
    "\\nwarrow" => Some(RelationSymbol)
    "\\odot" => Some(BinarySymbol)
    "\\ominus" => Some(BinarySymbol)
    "\\oplus" => Some(BinarySymbol)
    "\\origof" => Some(RelationSymbol)
    "\\oslash" => Some(BinarySymbol)
    "\\otimes" => Some(BinarySymbol)
    "\\owns" => Some(RelationSymbol)
    "\\parallel" => Some(RelationSymbol)
    "\\perp" => Some(RelationSymbol)
    "\\pitchfork" => Some(RelationSymbol)
    "\\pm" => Some(BinarySymbol)
    "\\prec" => Some(RelationSymbol)
    "\\precapprox" => Some(RelationSymbol)
    "\\preccurlyeq" => Some(RelationSymbol)
    "\\preceq" => Some(RelationSymbol)
    "\\precnapprox" => Some(RelationSymbol)
    "\\precneqq" => Some(RelationSymbol)
    "\\precnsim" => Some(RelationSymbol)
    "\\precsim" => Some(RelationSymbol)
    "\\propto" => Some(RelationSymbol)
    "\\restriction" => Some(RelationSymbol)
    "\\rhd" => Some(BinarySymbol)
    "\\rightarrow" => Some(RelationSymbol)
    "\\Rightarrow" => Some(RelationSymbol)
    "\\rightarrowtail" => Some(RelationSymbol)
    "\\rightharpoondown" => Some(RelationSymbol)
    "\\rightharpoonup" => Some(RelationSymbol)
    "\\rightleftarrows" => Some(RelationSymbol)
    "\\rightleftharpoons" => Some(RelationSymbol)
    "\\rightrightarrows" => Some(RelationSymbol)
    "\\rightsquigarrow" => Some(RelationSymbol)
    "\\rightthreetimes" => Some(BinarySymbol)
    "\\risingdotseq" => Some(RelationSymbol)
    "\\Rrightarrow" => Some(RelationSymbol)
    "\\Rsh" => Some(RelationSymbol)
    "\\rtimes" => Some(BinarySymbol)
    "\\searrow" => Some(RelationSymbol)
    "\\setminus" => Some(BinarySymbol)
    "\\shortmid" => Some(RelationSymbol)
    "\\shortparallel" => Some(RelationSymbol)
    "\\sim" => Some(RelationSymbol)
    "\\simeq" => Some(RelationSymbol)
    "\\smallfrown" => Some(RelationSymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_5(name : String) -> MathSymbolGroup? {
  match name {
    "\\smallsetminus" => Some(BinarySymbol)
    "\\smallsmile" => Some(RelationSymbol)
    "\\smile" => Some(RelationSymbol)
    "\\sqcap" => Some(BinarySymbol)
    "\\sqcup" => Some(BinarySymbol)
    "\\sqsubset" => Some(RelationSymbol)
    "\\sqsubseteq" => Some(RelationSymbol)
    "\\sqsupset" => Some(RelationSymbol)
    "\\sqsupseteq" => Some(RelationSymbol)
    "\\star" => Some(BinarySymbol)
    "\\subset" => Some(RelationSymbol)
    "\\Subset" => Some(RelationSymbol)
    "\\subseteq" => Some(RelationSymbol)
    "\\subseteqq" => Some(RelationSymbol)
    "\\subsetneq" => Some(RelationSymbol)
    "\\subsetneqq" => Some(RelationSymbol)
    "\\succ" => Some(RelationSymbol)
    "\\succapprox" => Some(RelationSymbol)
    "\\succcurlyeq" => Some(RelationSymbol)
    "\\succeq" => Some(RelationSymbol)
    "\\succnapprox" => Some(RelationSymbol)
    "\\succneqq" => Some(RelationSymbol)
    "\\succnsim" => Some(RelationSymbol)
    "\\succsim" => Some(RelationSymbol)
    "\\supset" => Some(RelationSymbol)
    "\\Supset" => Some(RelationSymbol)
    "\\supseteq" => Some(RelationSymbol)
    "\\supseteqq" => Some(RelationSymbol)
    "\\supsetneq" => Some(RelationSymbol)
    "\\supsetneqq" => Some(RelationSymbol)
    "\\swarrow" => Some(RelationSymbol)
    "\\therefore" => Some(RelationSymbol)
    "\\thickapprox" => Some(RelationSymbol)
    "\\thicksim" => Some(RelationSymbol)
    "\\times" => Some(BinarySymbol)
    "\\to" => Some(RelationSymbol)
    "\\triangleleft" => Some(BinarySymbol)
    "\\trianglelefteq" => Some(RelationSymbol)
    "\\triangleq" => Some(RelationSymbol)
    "\\triangleright" => Some(BinarySymbol)
    "\\trianglerighteq" => Some(RelationSymbol)
    "\\twoheadleftarrow" => Some(RelationSymbol)
    "\\twoheadrightarrow" => Some(RelationSymbol)
    "\\unlhd" => Some(BinarySymbol)
    "\\unrhd" => Some(BinarySymbol)
    "\\uparrow" => Some(RelationSymbol)
    "\\Uparrow" => Some(RelationSymbol)
    "\\updownarrow" => Some(RelationSymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_6(name : String) -> MathSymbolGroup? {
  match name {
    "\\Updownarrow" => Some(RelationSymbol)
    "\\upharpoonleft" => Some(RelationSymbol)
    "\\upharpoonright" => Some(RelationSymbol)
    "\\uplus" => Some(BinarySymbol)
    "\\upuparrows" => Some(RelationSymbol)
    "\\varpropto" => Some(RelationSymbol)
    "\\vartriangle" => Some(RelationSymbol)
    "\\vartriangleleft" => Some(RelationSymbol)
    "\\vartriangleright" => Some(RelationSymbol)
    "\\vdash" => Some(RelationSymbol)
    "\\vDash" => Some(RelationSymbol)
    "\\Vdash" => Some(RelationSymbol)
    "\\vee" => Some(BinarySymbol)
    "\\veebar" => Some(BinarySymbol)
    "\\Vvdash" => Some(RelationSymbol)
    "\\wedge" => Some(BinarySymbol)
    "\\wr" => Some(BinarySymbol)
    "←" => Some(RelationSymbol)
    "↚" => Some(RelationSymbol)
    "→" => Some(RelationSymbol)
    "↛" => Some(RelationSymbol)
    "↑" => Some(RelationSymbol)
    "↓" => Some(RelationSymbol)
    "↔" => Some(RelationSymbol)
    "↮" => Some(RelationSymbol)
    "↕" => Some(RelationSymbol)
    "↖" => Some(RelationSymbol)
    "↗" => Some(RelationSymbol)
    "↘" => Some(RelationSymbol)
    "↙" => Some(RelationSymbol)
    "↞" => Some(RelationSymbol)
    "↠" => Some(RelationSymbol)
    "↢" => Some(RelationSymbol)
    "↣" => Some(RelationSymbol)
    "↦" => Some(RelationSymbol)
    "↩" => Some(RelationSymbol)
    "↪" => Some(RelationSymbol)
    "↫" => Some(RelationSymbol)
    "↬" => Some(RelationSymbol)
    "↭" => Some(RelationSymbol)
    "↰" => Some(RelationSymbol)
    "↱" => Some(RelationSymbol)
    "↶" => Some(RelationSymbol)
    "↷" => Some(RelationSymbol)
    "↺" => Some(RelationSymbol)
    "↻" => Some(RelationSymbol)
    "↼" => Some(RelationSymbol)
    "↽" => Some(RelationSymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_7(name : String) -> MathSymbolGroup? {
  match name {
    "↾" => Some(RelationSymbol)
    "↿" => Some(RelationSymbol)
    "⇀" => Some(RelationSymbol)
    "⇁" => Some(RelationSymbol)
    "⇂" => Some(RelationSymbol)
    "⇃" => Some(RelationSymbol)
    "⇄" => Some(RelationSymbol)
    "⇆" => Some(RelationSymbol)
    "⇇" => Some(RelationSymbol)
    "⇈" => Some(RelationSymbol)
    "⇉" => Some(RelationSymbol)
    "⇊" => Some(RelationSymbol)
    "⇋" => Some(RelationSymbol)
    "⇌" => Some(RelationSymbol)
    "⇐" => Some(RelationSymbol)
    "⇍" => Some(RelationSymbol)
    "⇑" => Some(RelationSymbol)
    "⇒" => Some(RelationSymbol)
    "⇏" => Some(RelationSymbol)
    "⇓" => Some(RelationSymbol)
    "⇔" => Some(RelationSymbol)
    "⇎" => Some(RelationSymbol)
    "⇕" => Some(RelationSymbol)
    "⇚" => Some(RelationSymbol)
    "⇛" => Some(RelationSymbol)
    "⇝" => Some(RelationSymbol)
    "⇠" => Some(RelationSymbol)
    "⇢" => Some(RelationSymbol)
    "∈" => Some(RelationSymbol)
    "∋" => Some(RelationSymbol)
    "+" => Some(BinarySymbol)
    "±" => Some(BinarySymbol)
    "÷" => Some(BinarySymbol)
    "×" => Some(BinarySymbol)
    "<" => Some(RelationSymbol)
    "≮" => Some(RelationSymbol)
    "=" => Some(RelationSymbol)
    ">" => Some(RelationSymbol)
    "≯" => Some(RelationSymbol)
    "−" => Some(BinarySymbol)
    "∓" => Some(BinarySymbol)
    "∔" => Some(BinarySymbol)
    "∖" => Some(BinarySymbol)
    "∗" => Some(BinarySymbol)
    "∘" => Some(BinarySymbol)
    "∙" => Some(BinarySymbol)
    "∝" => Some(RelationSymbol)
    "∣" => Some(RelationSymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_8(name : String) -> MathSymbolGroup? {
  match name {
    "∤" => Some(RelationSymbol)
    "∦" => Some(RelationSymbol)
    "∧" => Some(BinarySymbol)
    "∨" => Some(BinarySymbol)
    "∩" => Some(BinarySymbol)
    "∪" => Some(BinarySymbol)
    "∴" => Some(RelationSymbol)
    "∵" => Some(RelationSymbol)
    "∼" => Some(RelationSymbol)
    "≁" => Some(RelationSymbol)
    "∽" => Some(RelationSymbol)
    "≀" => Some(BinarySymbol)
    "≂" => Some(RelationSymbol)
    "≃" => Some(RelationSymbol)
    "≅" => Some(RelationSymbol)
    "≆" => Some(RelationSymbol)
    "≈" => Some(RelationSymbol)
    "≊" => Some(RelationSymbol)
    "≍" => Some(RelationSymbol)
    "≎" => Some(RelationSymbol)
    "≏" => Some(RelationSymbol)
    "≐" => Some(RelationSymbol)
    "≑" => Some(RelationSymbol)
    "≒" => Some(RelationSymbol)
    "≓" => Some(RelationSymbol)
    "≖" => Some(RelationSymbol)
    "≗" => Some(RelationSymbol)
    "≜" => Some(RelationSymbol)
    "≡" => Some(RelationSymbol)
    "≤" => Some(RelationSymbol)
    "≰" => Some(RelationSymbol)
    "≥" => Some(RelationSymbol)
    "≱" => Some(RelationSymbol)
    "≦" => Some(RelationSymbol)
    "≧" => Some(RelationSymbol)
    "≨" => Some(RelationSymbol)
    "≩" => Some(RelationSymbol)
    "≪" => Some(RelationSymbol)
    "≫" => Some(RelationSymbol)
    "≬" => Some(RelationSymbol)
    "≲" => Some(RelationSymbol)
    "≳" => Some(RelationSymbol)
    "≶" => Some(RelationSymbol)
    "≷" => Some(RelationSymbol)
    "≺" => Some(RelationSymbol)
    "⊀" => Some(RelationSymbol)
    "≻" => Some(RelationSymbol)
    "⊁" => Some(RelationSymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_9(name : String) -> MathSymbolGroup? {
  match name {
    "≼" => Some(RelationSymbol)
    "⋠" => Some(RelationSymbol)
    "≽" => Some(RelationSymbol)
    "⋡" => Some(RelationSymbol)
    "≾" => Some(RelationSymbol)
    "≿" => Some(RelationSymbol)
    "⊂" => Some(RelationSymbol)
    "⊃" => Some(RelationSymbol)
    "⊆" => Some(RelationSymbol)
    "⊈" => Some(RelationSymbol)
    "⊇" => Some(RelationSymbol)
    "⊉" => Some(RelationSymbol)
    "⊊" => Some(RelationSymbol)
    "⊋" => Some(RelationSymbol)
    "⊎" => Some(BinarySymbol)
    "⊏" => Some(RelationSymbol)
    "⊐" => Some(RelationSymbol)
    "⊑" => Some(RelationSymbol)
    "⊒" => Some(RelationSymbol)
    "⊓" => Some(BinarySymbol)
    "⊔" => Some(BinarySymbol)
    "⊕" => Some(BinarySymbol)
    "⊖" => Some(BinarySymbol)
    "⊗" => Some(BinarySymbol)
    "⊘" => Some(BinarySymbol)
    "⊙" => Some(BinarySymbol)
    "⊚" => Some(BinarySymbol)
    "⊛" => Some(BinarySymbol)
    "⊝" => Some(BinarySymbol)
    "⊞" => Some(BinarySymbol)
    "⊟" => Some(BinarySymbol)
    "⊠" => Some(BinarySymbol)
    "⊡" => Some(BinarySymbol)
    "⊢" => Some(RelationSymbol)
    "⊬" => Some(RelationSymbol)
    "⊣" => Some(RelationSymbol)
    "⊨" => Some(RelationSymbol)
    "⊭" => Some(RelationSymbol)
    "⊩" => Some(RelationSymbol)
    "⊮" => Some(RelationSymbol)
    "⊪" => Some(RelationSymbol)
    "⊯" => Some(RelationSymbol)
    "⋬" => Some(RelationSymbol)
    "⋭" => Some(RelationSymbol)
    "⊶" => Some(RelationSymbol)
    "⊷" => Some(RelationSymbol)
    "⊸" => Some(RelationSymbol)
    "⊺" => Some(BinarySymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_10(name : String) -> MathSymbolGroup? {
  match name {
    "⊻" => Some(BinarySymbol)
    "⊼" => Some(BinarySymbol)
    "⋅" => Some(BinarySymbol)
    "⋇" => Some(BinarySymbol)
    "⋈" => Some(RelationSymbol)
    "⋉" => Some(BinarySymbol)
    "⋊" => Some(BinarySymbol)
    "⋋" => Some(BinarySymbol)
    "⋌" => Some(BinarySymbol)
    "⋍" => Some(RelationSymbol)
    "⋎" => Some(BinarySymbol)
    "⋏" => Some(BinarySymbol)
    "⋐" => Some(RelationSymbol)
    "⋑" => Some(RelationSymbol)
    "⋒" => Some(BinarySymbol)
    "⋓" => Some(BinarySymbol)
    "⋔" => Some(RelationSymbol)
    "⋘" => Some(RelationSymbol)
    "⋙" => Some(RelationSymbol)
    "⋚" => Some(RelationSymbol)
    "⋛" => Some(RelationSymbol)
    "⋞" => Some(RelationSymbol)
    "⋟" => Some(RelationSymbol)
    "⋦" => Some(RelationSymbol)
    "⋧" => Some(RelationSymbol)
    "⋨" => Some(RelationSymbol)
    "⋩" => Some(RelationSymbol)
    "⌢" => Some(RelationSymbol)
    "⌣" => Some(RelationSymbol)
    "◯" => Some(BinarySymbol)
    "⟵" => Some(RelationSymbol)
    "⟶" => Some(RelationSymbol)
    "⟷" => Some(RelationSymbol)
    "⟸" => Some(RelationSymbol)
    "⟹" => Some(RelationSymbol)
    "⟺" => Some(RelationSymbol)
    "⟼" => Some(RelationSymbol)
    "⩞" => Some(BinarySymbol)
    "⩽" => Some(RelationSymbol)
    "⩾" => Some(RelationSymbol)
    "⪅" => Some(RelationSymbol)
    "⪆" => Some(RelationSymbol)
    "⪇" => Some(RelationSymbol)
    "⪈" => Some(RelationSymbol)
    "⪉" => Some(RelationSymbol)
    "⪊" => Some(RelationSymbol)
    "⪋" => Some(RelationSymbol)
    "⪌" => Some(RelationSymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group_11(name : String) -> MathSymbolGroup? {
  match name {
    "⪕" => Some(RelationSymbol)
    "⪖" => Some(RelationSymbol)
    "⪯" => Some(RelationSymbol)
    "⪰" => Some(RelationSymbol)
    "⪵" => Some(RelationSymbol)
    "⪶" => Some(RelationSymbol)
    "⪷" => Some(RelationSymbol)
    "⪸" => Some(RelationSymbol)
    "⪹" => Some(RelationSymbol)
    "⪺" => Some(RelationSymbol)
    "⫅" => Some(RelationSymbol)
    "⫆" => Some(RelationSymbol)
    "⫋" => Some(RelationSymbol)
    "⫌" => Some(RelationSymbol)
    _ => None
  }
}

///|
fn builtin_math_symbol_group(name : String) -> MathSymbolGroup? {
  match builtin_math_symbol_group_0(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_1(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_2(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_3(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_4(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_5(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_6(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_7(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_8(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_9(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_10(name) {
    Some(group) => return Some(group)
    None => ()
  }
  match builtin_math_symbol_group_11(name) {
    Some(group) => return Some(group)
    None => ()
  }
  None
}
